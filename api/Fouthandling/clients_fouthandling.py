
class ClientsFoutHandling:

    def check_get_client():
        # check on negatieve IDs
        return

    def check_add_client():
        # Check wheter the sent JSON body matches the body of a client object
        # Check whether the ID in the JSON body already exists in the database or not. Voorkom dubbele IDS
        return

    def check_put_client():
        # Make sure the ID in the JSON body is the same als van de client body die je wilt verandered
        return

    def check_remove_client():
        # Check pp negatieve ID's
        return
